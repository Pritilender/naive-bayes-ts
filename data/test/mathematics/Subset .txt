mathematics	subset navigation search superset euler diagram mathematics especially set theory set subset set equivalently superset contained inside that all elements are also elements and may the relationship one set being subset another called inclusion sometimes containment the subset relation defines partial order the algebra subsets forms boolean algebra which the subset relation called inclusion contents definitions the symbols and examples other properties inclusion see also references external links definitions and are sets and every element also element then subset included denoted superset denoted subset but not equal there exists least one element which not element then also proper strict subset this written proper superset this written for any set the inclusion relation partial order the set all subsets the power set when represented the symbols and some authors use the symbols and indicate subset and superset respectively instead the symbols and but with the same for for these true every set that other authors prefer use the symbols and indicate proper subset and respectively place and this usage makes and analogous the inequality symbols and for then may equal maybe but then definitely does not equal and strictly less than similarly using the means proper convention then may may not equal but then definitely not equal examples the set proper subset any set subset but not proper the empty set denoted also subset any given set also always proper subset any set except the set prime number greater than proper subset odd number greater than the set natural numbers proper subset the set rational numbers and the set points line segment proper subset the set points line these are examples which both the part and the whole are and the part has the same cardinality number the such cases can tax other properties inclusion inclusion the canonical partial order the sense that every partially ordered set isomorphic some collection sets ordered the ordinal numbers are simple each ordinal identified with the set all ordinals less than equal then and only for the power set set the inclusion partial order order isomorphism the cartesian product the cardinality copies the partial order for which this can illustrated enumerating and associating with each subset which say with each element the tuple from which the coordinate and only member see also containment order 